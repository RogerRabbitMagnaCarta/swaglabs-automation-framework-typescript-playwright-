# existing entries…
BASE_URL=https://www.saucedemo.com/v1/
DB_PATH=test/db/data.db
BROWSER=chromium
HEADLESS=true

# timeouts (in milliseconds)
STEP_TIMEOUT=60000        # default per-step timeout: 10s
LOGIN_TIMEOUT=50000       # override for login step: 60s
